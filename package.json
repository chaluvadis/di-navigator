{
  "name": "di-service-navigator",
  "displayName": "di-service-navigator",
  "description": "Visualize and navigate your .NET Dependency Injection registrations inside VSCode.",
  "version": "0.0.1",
  "icon": "./icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chaluvadis/di-navigator"
  },
  "homepage": "https://github.com/chaluvadis/di-navigator",
  "bugs": {
    "url": "https://github.com/chaluvadis/di-navigator/issues"
  },
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Other",
    "Programming Languages",
    "Visualization"
  ],
  "activationEvents": [
    "onLanguage:csharp",
    "onWorkspaceContains:*.csproj",
    "onWorkspaceContains:*.sln"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "di-navigator.refreshServices",
        "title": "Refresh DI Services"
      },
      {
        "command": "di-navigator.goToImplementation",
        "title": "Go to DI Implementation"
      }
    ],
    "views": {
      "explorer": [
        {
          "id": "diNavigator",
          "name": "DI Navigator",
          "icon": "./icon.png"
        }
      ]
    }
  },
  "configuration": {
    "title": "DI Navigator",
    "properties": {
      "diNavigator.excludeFolders": {
        "type": "array",
        "default": [
          "**/bin/**",
          "**/obj/**",
          "**/Properties/**"
        ],
        "description": "Glob patterns for folders to exclude from file scanning (e.g., build artifacts).",
        "items": {
          "type": "string"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.104.0",
    "@typescript-eslint/eslint-plugin": "^8.43.0",
    "@typescript-eslint/parser": "^8.43.0",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.9",
    "eslint": "^9.35.0",
    "npm-run-all": "^4.1.5",
    "tree-sitter": "^0.21.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "tree-sitter-c-sharp": "^0.23.1"
  }
}